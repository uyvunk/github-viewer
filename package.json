{
  "name": "github-viewer",
  "private": true,
  "version": "0.0.0",
  "description": "An example of using AngularJS to view github profiles",
  "repository": "https://github.com/angular/angular-phonecat",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.9.0"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server ./app -a localhost -p 8000 -c-1"
  }
}